```{r}
library(data.table)

inst = as.data.table(installed.packages()[,c("Package", "LibPath")])
inst


dt = as.data.table(old.packages()[,c("Package", "LibPath")])
dt

dt[LibPath == .libPaths()[2]]

# Update these, if any
dt[LibPath == .libPaths()[2]][!inst, on =.(Package)]



# do not upgrade
# these from 2, were updated earlier to 1
dt[LibPath == .libPaths()[2]][inst[LibPath == .libPaths()[1]], on =.(Package), nomatch=NULL]
```
